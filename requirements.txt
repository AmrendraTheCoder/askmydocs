# --- backend ---
fastapi
uvicorn[standard]
python-multipart        # lets FastAPI accept file uploads

# --- AI / search ---
sentence-transformers   # turns text into numbers (embeddings)
chromadb                # the vector database
rank-bm25               # old-school keyword search

# --- reading files ---
pypdf                   # read text out of PDFs

# --- computer vision ---
opencv-python           # image processing
pytesseract             # OCR (reading text from an image)
Pillow                  # image loading helper

# --- misc ---
requests                # call APIs from python
numpy
